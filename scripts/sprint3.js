'use strict';
//Non obbligatorio usare questo listener
/*
document.addEventListener("DOMContentLoaded", async function(event) {
    let j = 0;
    for (let key in db.marketingMix) {
        j=j+1;
        //console.log(key);
        //onsole.log(db.marketingMix[key].input);
        document.getElementById("Input_MM".concat(j.toString())).innerHTML = db.marketingMix[key].input;
    }
});*/


function correggi() {
    console.log("Pulisci");
}